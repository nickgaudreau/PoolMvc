@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { Area = "Common", id = "logoutForm", @class = "navbar-right" }))
    {
    @Html.AntiForgeryToken()

    <ul class="nav navbar-nav navbar-right">
        <li>
            @Html.ActionLink("Logged in as " + User.Identity.GetUserName() + "!", "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage" })
        </li>
        <li><a href="javascript:document.getElementById('logoutForm').submit()">Log off</a></li>
    </ul>
    }
}
else
{
    <ul class="nav navbar-nav navbar-right">
        @*<li>@Html.ActionLink("Register", "RegisterRedirect", "Home", routeValues: null, htmlAttributes: new { Area = "Common", id = "registerLink" })</li>*@
        <li>@Html.ActionLink("Log in", "LoginRedirect", "Home", routeValues: null, htmlAttributes: new { Area = "Common", id = "loginLink" })</li>
    </ul>
}

